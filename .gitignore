
# Eclipse-specific files
.classpath        ← Eclipse project metadata
.project          ← Eclipse project metadata
.settings/        ← Folder with Eclipse preferences

# Java build outputs
bin/              ← Compiled `.class` files usually go here

# Maven or Gradle build directories
target/           ← Maven builds output here
build/            ← Gradle builds output here

# OS junk files (created automatically by Windows/macOS)
.DS_Store         ← macOS hidden file
Thumbs.db         ← Windows hidden image cache file
